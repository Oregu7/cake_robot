const cartCommand = require("./cartCommand");
const productCommand = require("./productCommand");
const payCommand = require("./payCommand");

module.exports = {
    cartCommand,
    productCommand,
    payCommand,
};